{
  "name": "jgent",
  "displayName": "jgent",
  "description": "JR Agent",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.102.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": ["onCommand:jgent.askAgent"],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "jgent.askAgent",
      "title": "Ask Agent"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.102.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "eslint": "^9.25.1",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  },
    "repository": {
    "type": "git",
    "url": "https://github.com/your-name/myex.git"
  }
}
